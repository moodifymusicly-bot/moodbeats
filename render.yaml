services:
  - type: web
    name: moodbeatz-backend
    env: docker
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: APP_NAME
        value: MoodBeatz API
      - key: ALLOWED_ORIGINS
        value: https://moodbeatzz.vercel.app,http://localhost:3000
      - key: PORT
        value: 8000
